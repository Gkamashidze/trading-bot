[build]
builder = "dockerfile"

[deploy]
startCommand = "alembic upgrade head; python -m trading_bot.main"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
